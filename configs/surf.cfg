exec default.cfg

// Air acceleration changes to make surfing possible
// NOTE: sourcemod must be installed for this to work
sm_cvar sv_airaccelerate 100

// Disable respawn timers
mp_disable_respawn_times 1
